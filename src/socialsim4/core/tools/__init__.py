"""Tooling package for agent actions.

This package will host reusable tool implementations (web, social, etc.) that
actions can call. Keep side effects minimal and return plain data structures.
"""

